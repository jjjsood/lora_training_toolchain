# L-F — reference adapter: all adapted linears, blocks 0-23, rank 16.
# The "normal" case every other STYLE-block adapter is compared against.
adapter_id: L-F
extends: ../base.yaml
overrides_allowed: [target]
target:
  scope: transformer
  blocks: [0, 23]
  module_classes: [attn, mlp]
  rank: 16
  alpha: 16

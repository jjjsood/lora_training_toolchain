# Community FLUX checkpoint survey (CAP-28).
# Two FLUX checkpoints — one style LoRA, one subject LoRA — each must pass
# weight introspection and the FLUX E_img gate before use.
base_arch: flux
gate_config: ../gate/e_img_flux.yaml
targets:
  - slot: CF-1  # style LoRA
    source: civitai
    status: pending_acquisition
  - slot: CF-2  # subject LoRA
    source: civitai
    status: pending_acquisition
fallback:
  min_usable: 2
  on_shortfall: use_fallback_configs
